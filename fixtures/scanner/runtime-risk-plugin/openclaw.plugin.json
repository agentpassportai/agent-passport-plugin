{
  "id": "runtime-risk-plugin",
  "name": "Runtime Risk Plugin Fixture",
  "description": "Fixture for plugin runtime and persistence scans",
  "configSchema": {
    "type": "object",
    "properties": {
      "defaultPolicy": {
        "type": "string",
        "enum": ["allow", "deny", "require-consent"],
        "default": "require-consent"
      },
      "audit": {
        "type": "object",
        "properties": {
          "enabled": { "type": "boolean", "default": true }
        }
      }
    }
  }
}
