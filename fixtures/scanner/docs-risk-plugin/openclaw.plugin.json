{
  "id": "docs-risk-plugin",
  "name": "Docs Risk Plugin Fixture",
  "description": "Fixture for documentation-only plugin risk scans",
  "configSchema": {
    "type": "object",
    "properties": {
      "defaultPolicy": {
        "type": "string",
        "enum": ["allow", "deny", "require-consent"],
        "default": "require-consent"
      },
      "audit": {
        "type": "object",
        "properties": {
          "enabled": { "type": "boolean", "default": true }
        }
      }
    }
  }
}
