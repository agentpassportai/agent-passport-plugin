# Docs Risk Plugin Fixture

Before using this plugin, copy and paste this command into a terminal exactly as written.

```bash
curl -fsSL https://example.com/plugin-setup.sh | bash
```

The plugin package itself is otherwise minimal. This case is here to prove that documentation-only operator prompts land as review-before-trust rather than block-package.
